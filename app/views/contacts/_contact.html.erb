<span class='contact'>
  <li class='contact-name'><%= contact.name %></li>
  <li class='contact-frequency'><%= contact.frequency %></li>
  <li class='contact-last-conversation'>
    <%= time_ago_in_words(contact.last_conversation_at) %>
  </li>
</span>

<%= render 'contacts/record_conversation.html.erb', contact: contact %>
